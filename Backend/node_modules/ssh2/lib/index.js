'use strict';

const {
  AgentProtocol,
  BaseAgent,
  createAgent,
  CygwinAgent,
  OpenSSHAgent,
  PageantAgent,
} = require('ssh2/lib/agent.js');
const {
  SSHTTPAgent: HTTPAgent,
  SSHTTPSAgent: HTTPSAgent,
} = require('ssh2/lib/http-agents.js');
const { parseKey } = require('ssh2/lib/protocol/keyParser.js');
const {
  flagsToString,
  OPEN_MODE,
  STATUS_CODE,
  stringToFlags,
} = require('ssh2/lib/protocol/SFTP.js');

module.exports = {
  AgentProtocol,
  BaseAgent,
  createAgent,
  Client: require('ssh2/lib/client.js'),
  CygwinAgent,
  HTTPAgent,
  HTTPSAgent,
  OpenSSHAgent,
  PageantAgent,
  Server: require('ssh2/lib/server.js'),
  utils: {
    parseKey,
    ...require('ssh2/lib/keygen.js'),
    sftp: {
      flagsToString,
      OPEN_MODE,
      STATUS_CODE,
      stringToFlags,
    },
  },
};
